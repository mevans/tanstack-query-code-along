import { CheckboxValueDirective } from './checkbox-value.directive';

describe('CheckboxValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxValueDirective();
    expect(directive).toBeTruthy();
  });
});
